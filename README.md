
    - 1️⃣ What is the difference between var, let, and const?
answer : 1 : var hosted ,
            let and const hosted but temporal Dead zone , 
        2 : let and const what the same name multiple variable cannot be declared 
            but var multiple exits with the same name can be declared
        3 : var and let variable value can be changed
           but const variable value cannot change 
        4: var cannot maintain block scope 
           but let and const maintain block scope 
    
    - 2️⃣ What is the spread operator (...)?
 answer: a array or object takes out all the contents and separates them

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
answer: 1 : map() updates elements 
        2 : filter() condition match element find out 
            when condition no match [] will give
        3 : forEach() array  every elements on by iterate 

    - 4️⃣ What is an arrow function?
answer : arrow function => 
    
    - 5️⃣ What are template literals?
answer : A new powerful way to declare string in javaScript 
         (``) multiple string can be used template string 
         and variable can be written inside







